{
  "name": "tennu-correction",
  "version": "3.0.1",
  "description": "A plugin for the Tennu IRC framework. Search and replace text in previous messages.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tennu/tennu-correction"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "irc-colors": "^1.2.0"
  }
}
