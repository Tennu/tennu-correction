{
  "name": "tennu-correction",
  "version": "2.0.1",
  "description": "A plugin for the Tennu IRC framework. Corrects previous messages. Requires Admin, and dbcore, and dblogger.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/LordWingZero/tennu-correction"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "irc-colors": "^1.2.0",
    "moment": "^2.10.6",
    "request-promise": "^1.0.2"
  }
}
